import { Component, Input } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'number-input-panel',
  templateUrl: 'number-input-panel.html',
  styleUrls: ['number-input-panel.scss']
})

export class NumberInputPanel {
}
